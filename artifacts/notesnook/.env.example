# When deploying the frontend on a different origin than the API server
# (e.g. GitHub Pages frontend + Replit API), set this to the API server's
# base URL. The frontend appends "/api" automatically.
#
# Example:
# VITE_API_URL=https://ballpointone.replit.app
